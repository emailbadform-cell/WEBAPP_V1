# V2.24.4 / V10.34 — Forward UI Cleanup

Display-only patch.

- Corrected the actual live Forward path in `advanced_signals.py`.
- Internal `REVERSAL` phase is still retained for research/logging.
- Main Forward labels now render as `UP — CONTINUATION`, `DOWN — CONTINUATION`, `UP`, `DOWN`, or `SIDEWAYS`.
- Removed the duplicate Forward Signal group from Advanced Diagnostics.
- Main Forward Signal tile remains visible and active.
- V2.24.1 health-check fix, V2.24.2 Progress fix, and all V2.24 behavior retained.
- Profit Protection and all predictive model logic unchanged.
