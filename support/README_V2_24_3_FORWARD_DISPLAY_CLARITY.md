# BTC15M Web V2.24.3 / V10.34 — Forward Display Clarity

UI-only Forward Signal wording patch.

- `UP — CONTINUATION` remains `UP — CONTINUATION`.
- `DOWN — CONTINUATION` remains `DOWN — CONTINUATION`.
- Internal `UP — REVERSAL` displays as `UP`.
- Internal `DOWN — REVERSAL` displays as `DOWN`.
- `SIDEWAYS` remains `SIDEWAYS`.
- Internal phase classification remains `CONTINUATION` or `REVERSAL` for research/logging.
- No Forward model, threshold, Decision, GT, Profit Protection, TR, MP, OF, L2, timer, chart, P/L, health-check, or progress logic changes.
